<h2>Create a new note for this resume entry</h2>
<%= form_for(@resume_entry_note) do |f| %>
	<%= render 'shared/resume_entry_note_form_fields', :f => f %>
	<div class="actions">
		<%= f.submit "Create New Note" %>
	</div>
<% end %>