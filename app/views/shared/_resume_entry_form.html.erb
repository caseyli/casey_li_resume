<h2>Add a new Resume Entry</h2>
<%= form_for(@resume_entry) do |f| %>
	<%= render 'shared/resume_entry_form_fields', :f => f %>
	<div class="actions">
		<%= f.submit "Create New Entry" %>
	</div>
<% end %>