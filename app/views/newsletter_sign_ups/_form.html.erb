<%= form_for(@newsletter_sign_up) do |f| %>
  <% if @newsletter_sign_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newsletter_sign_up.errors.count, "error") %> prohibited this newsletter_sign_up from being saved:</h2>

      <ul>
      <% @newsletter_sign_up.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
